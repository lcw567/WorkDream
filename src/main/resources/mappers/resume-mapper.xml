<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ResumeMapper">

    
    <insert id="insertResume" parameterType="com.cs.workdream.resume.model.vo.Resume" useGeneratedKeys="true" keyProperty="resumeNo">
    INSERT INTO RESUME (
        RESUME_NO, PERSON_NO, RESUME_TITLE, CREATE_DATE, MODIFIED_DATE, DELETED,
        USER_NAME, USER_GENDER, USER_BIRTH, USER_EMAIL, USER_PHONE, USER_ADDRESS,
        USER_ROAD_ADDRESS, USER_DETAIL_ADDRESS, USER_PIC, EDUCATION_LEVEL,
        ACADEMIC_NAME, MAJOR, DEGREE, STATUS, ENTER_DATE, GRADUATION_DATE,
        LOCATION, EXAM_PASSED, SKILL, ACTICITY_TYPE, ORGANIZATION_NAME,
        START_DATE_ACT, END_DATE_ACT, DESCRIPTION, CATEGORY, NAME, ISSUED_BY,
        PROFICIENCY_LEVEL, KIND_OF_LANGUAGE, PASSING_CATEGORY, AWARD_NAME,
        ORGANIZER, AWARD_DATE, ISSUE_DATE, VETERAN_REASON, SERVICE_STATE,
        UNFULFILLED_REASON, EXEMPTED_REASON, ENLISTMENT_DATE, DISCHARGE_DATE,
        MILITARY_SELECTION, CLASS_SELECTION, DISCHARGE_REASON, WORK, DEPARTMENT,
        COMPANY_TITLE, START_DATE_WORK, END_DATE_WORK, STATUS_WORK, JOB_CONTENT,
        POSITION
    )
    VALUES (
        RESUME_SEQ.NEXTVAL, #{personNo}, #{resumeTitle}, SYSDATE, SYSDATE, 'N',
        #{userName}, #{userGender}, #{userBirth}, #{userEmail}, #{userPhone}, #{userAddress},
        #{userRoadAddress}, #{userDetailAddress}, #{userPic}, #{educationLevel},
        #{academicName}, #{majorName}, #{degree}, #{graduationStatus}, #{enterDate},
        #{graduationDate}, #{location}, #{examPassed}, #{skillName},
        #{activityType}, #{organizationName}, #{startDateAct}, #{endDateAct},
        #{description}, #{category}, #{name}, #{issuingAgency},
        #{proficiencyLevel}, #{languageType}, #{passStatus}, #{awardName},
        #{organizer}, #{awardDate}, #{issueDate}, #{veteranReason}, #{serviceStatus},
        #{unfinishedReason}, #{exemptionReason}, #{enlistmentDate}, #{dischargeDate},
        #{militaryBranch}, #{rank}, #{dischargeReason}, #{work}, #{department},
        #{companyTitle}, #{startDateWork}, #{endDateWork}, #{careerStatus}, #{jobContent},
        #{position}
    )
</insert>


    <!-- 기타 삽입 쿼리 (필요 시 추가) -->

</mapper>
